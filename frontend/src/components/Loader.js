import React from "react";

import '../styles/sass/loader.scss'

const Loader = () => {
  return (

    <div class="spin-wrapper">
      <div class="spinner">
      </div>
    </div>

  );
};

export default Loader;

